# 参考资料索引（按天累积）

> 建立时间：2026-09-10。
> **使用原则：官方文档优先 → 库内已评级资源次之 → 问 AI 排第三。**
> 理由见 `AI使用规则.md` 的「查文档 vs 问 AI 的顺序」一节。

## 链接验证状态说明

| 标记 | 含义 |
|---|---|
| ✅ 实测 | 我实际请求过，返回 200（**2026-09-10 二次验证，开了代理后重测**） |
| 📋 库内快照 | 来自本库 2026-09-10 的抓取记录，我无法验证 |
| ⚠️ | 有坑，注意看备注 |

> **二次验证结果（09-10 晚）**：开代理后 GitHub 全部可达。
> 我**不只测了状态码，还把每个仓库的真实自述拉了下**（GitHub API 被墙得抖，
> 改用解析页面），结论：**库里 `05-学习资料与可信度评级.md` 对这些仓库的描述是准确的**。

---

# Day 2：dict / JSON / 异常 / 类

## 1. dict 计数（练习 1）

**去哪**：Python 官方文档中文版 → 教程 → 数据结构 ✅ 实测

https://docs.python.org/zh-cn/3/tutorial/datastructures.html

**看哪一节**：往下滚到「5.5 字典」

**必须搞懂的 3 个点**：
- `d[key]` 和 `d.get(key, 默认值)` 的区别 —— 前者 key 不存在会 `KeyError` 直接崩
- `.items()` 返回什么（为什么能 `for k, v in d.items()`）
- `key in d` 判断存在性（比 `d.keys()` 快）

> 💡 你昨天写脚本时如果用过 `counts[w] = counts.get(w, 0) + 1` 这行 ——
> 它就是"用 `.get` 带默认值"的经典写法，文档里能找到它的正式名字。

---

## 2. `sorted(key=)` —— 为什么 `-kv[1]` 能让次数从大到小（练习 1）

**去哪**：Python 官方文档中文版 → 排序指南 ✅ 实测

https://docs.python.org/zh-cn/3/howto/sorting.html

**看哪一节**：「Key 函数」和「升序和降序」

**这一页专门就是回答你这个问题的**。看完你就知道：
- `sorted(x, key=f)` 里的 `f` 是"给每个元素算一个排序用的分数"
- 元组比较是**从左往右逐个比** —— 这就是为什么 `(-次数, 单词)` 能同时实现"次数降序 + 单词升序"
- `lambda kv: (-kv[1], kv[0])` 里，`kv` 是一个 `(单词, 次数)` 元组

> ⚠️ 想不通 **先照抄跑通**，再回来读这一页。**不要卡在理解上不动手**。

---

## 3. JSON 读写（练习 2）

**去哪**：Python 官方文档中文版 → json 模块 ✅ 实测

https://docs.python.org/zh-cn/3/library/json.html

**看哪一节**：页面**最上面那张对照表**（最重要）

**必须分清这四个**（高频混淆点，面试也爱问）：

| 函数 | 作用 | 方向 |
|---|---|---|
| `json.dumps(obj)` | Python 对象 → **字符串** | 内存 |
| `json.dump(obj, f)` | Python 对象 → **文件** | 落盘 |
| `json.loads(s)` | **字符串** → Python 对象 | 内存 |
| `json.load(f)` | **文件** → Python 对象 | 读盘 |

**记忆法**：**带 `s` 的是 String**。

**另外注意两件事**（顺手就学会，以后省很多事）：
- `ensure_ascii=False` —— 不写的话中文会变成 `\u5f20\u4e09` 这种转义
- JSON 里**没有元组**，Python 的元组存进去会变成列表 —— 这就是练习 5 为什么要 `list(t)`

---

## 4. 异常处理（练习 3）

**去哪**：Python 官方文档中文版 → 错误和异常 ✅ 实测

https://docs.python.org/zh-cn/3/tutorial/errors.html

**看哪一节**：「8.3 处理异常」「8.4 抛出异常」

**必须搞懂的**：
- `try / except / else / finally` 四段各在什么时候执行（`else` 和 `finally` 很少人用对，面试会问）
- 怎么一次抓多种异常：`except (FileNotFoundError, json.JSONDecodeError):`
- `except Exception:` 为什么能兜住大部分错，以及**为什么不要写裸的 `except:`**

**要查"某个错误叫什么名字"时**：官方文档 → 库参考 → 内置异常
https://docs.python.org/zh-cn/3/library/exceptions.html

---

## 5. 类（练习 4）⭐ 今天的重点

**去哪**：Python 官方文档中文版 → 类 ✅ 实测

https://docs.python.org/zh-cn/3/tutorial/classes.html

**看哪一节**：「9.3 初探类」（**只看这一节就够**）

**重点看它对 `self` 的解释**，然后配合任务卡里那张 C 语言类比表：

| Python | C |
|---|---|
| `self` | 指向自己的指针（`&conv`） |
| `self._messages` | 结构体成员 |
| `conv.add_user(x)` | `add_user(&conv, x)` |

**⚠️ 今天不要看这些**（会劝退，而且现在用不上）：
- 继承、多继承、MRO
- 元类（metaclass）、`__slots__`
- 装饰器进阶（`@property` 只要会"加一行就能不加括号调用"就够了，原理 Day 3 讲）

> 判断标准：**看到看不懂的章节，直接跳过。文档不是教科书，是工具。**

---

## 6. 力扣 LC 1 / LC 9

**先自己写 25 分钟**，卡住了再：

1. 点开「题解」标签（**不要直接看官方答案**）
2. 只看**思路**那一段，不看完整代码
3. 关掉题解，**从空白重写一遍**
4. 重写完还错，再看代码

**题解区额外看什么**：评论区里别人**踩过的坑**（比如 LC 1 里"同一个元素不能用两次"、
LC 9 里"负数"和"末尾是 0"）

---

# 库内已评级资源（长期用）

| 资源 | 什么时候用 | 链接 | 实测结果 |
|---|---|---|---|
| **iswbm/python-guide** ⭐ | **Python 基础主力**：语法查漏、零基础补课 | https://github.com/iswbm/python-guide | ✅ 200 · 仓库自述：「Python中文指南：**从零到一的零基础 Python 教程**」 |
| **llm-universe**（T0 ③） | 第 1-7 周过渡教材（边学 Python 边接触大模型） | https://github.com/datawhalechina/llm-universe | ✅ 200 · 自述：「面向**小白开发者**的大模型应用开发教程」 |
| ↳ 同上·在线阅读版 | 不想 clone 就直接看 | https://datawhalechina.github.io/llm-universe/ | ✅ 200 |
| PegasusWang/python_data_structures_and_algorithms | Day 3 之后补数据结构 / 算法 | https://github.com/PegasusWang/python_data_structures_and_algorithms | ✅ 200 · 自述：「Python 中文**数据结构和算法**教程」 |
| zhongyi-tong/git-recipes | **Day 4 要用**：Git 中文教程 | https://github.com/zhongyi-tong/git-recipes | ✅ 200 · 自述：「**高质量的 Git 中文教程**」 |

### ⚠️ 更正我之前说错的一句话

我早先说"**库里缺 Python 基础资源**" —— **说错了**。

`iswbm/python-guide` 的仓库自述就是「从零到一的零基础 Python 教程」，正好填这个位置。
而且库里 `13-12周详细周计划.md` 第 1 周本来就列了它 —— 是我一开始没注意到。

### ❌ 廖雪峰 Python 教程：验不过，不推荐

我试了两个地址，**都连不上（URLError）**：

- `https://liaoxuefeng.com/books/python/introduction/index.html`
- `https://www.liaoxuefeng.com/wiki/1016959663602400`

可能是我的网络环境问题，也可能是站点问题。**但没验过的东西我不给你**。
要补 Python 基础，用上面实测过的 `iswbm/python-guide`。

---

# 查资料时的心态

调研里 B站热评那句话值得每个月重读一次：

> **"收藏从未停止，学习从未开始。"**

所以这里的规则是：**一次只开一个来源，看完必须动手。**
今天你只需要用到一个 —— **Python 官方文档的「数据结构」和「类」两页**。
