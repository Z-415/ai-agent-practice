# 代码审查记录（累积）

> 这份文档会一直增长。**第 12 周写简历、准备面试时，直接回来翻这个文件。**
> 每条审查意见只要重复出现 2 次以上，就说明是你需要专门改掉的习惯。

---

## Day 1 审查（2026-09-10）

**结论：功能全部正确，6 道题全对。** 下面是"能更好"的部分。

### 1. ⚠️ 分号是 C 的习惯，Python 不需要

```python
total = 0;          # 你写的
total = 0           # Python 的写法
```

不报错，但一眼就能看出"这是写 C 的人写的 Python"。面试官看简历项目代码时会注意到。

### 2. ⚠️ 运算符两边要空格（这条最重要）

```python
quotient = 17//5        # 你写的
score>=90               # 你写的
i%3==0                  # 你写的

quotient = 17 // 5      # 规范写法（PEP 8）
score >= 90
i % 3 == 0
```

为什么现在就要改：代码短的时候无所谓，**超过 100 行以后，`a=b*c-d/e` 这种代码自己都看不懂**。
你要写的 Agent 项目单个文件动不动 300 行以上，这个习惯必须现在改。

> 💡 顺手做：在 VS Code 里装 **Ruff** 扩展（或 Black），保存时自动格式化，以后不用自己操心。

### 3. 🔥 自测通过 ≠ 完全符合要求（这节课最重要）

练习 2 的要求是"拼一句话，**里面要包含上面 4 个变量的值**"。
你写的是：

```python
intro = f"我是{name},今年{age}岁"      # 只用了 2 个变量
```

**但我的自测没抓出来。** 这是我的测试写得不够严，也是你要记住的一个教训：

> **评测集有漏洞，测试就会给你假的信心。**

这不是空话——第 10 周你会花整整一天给 RAG 项目"建评测集 + 算召回率"，
第 12 周还要给 Agent 做"20 条用例 + LLM-as-judge 成功率"，**就是干这件事**。
今天你已经踩到了这个坑的原始版本。

### 4. 死代码要清掉

```python
total = 0
for i in range(1, 101):
    total += i
        # TODO        # ← 这几行留着没用了

def fizzbuzz(n):
    ...
    return result
        # TODO        # ← pass 在 return 后面，永远执行不到
    pass
```

`return` 之后的代码永远不执行。功能上无害，但会让读代码的人困惑。

### 5. 知道多种写法（面试加分项）

```python
# 你写的（完全正确，而且是面试官想看的"你会不会循环"）
total = 0
for i in range(1, 101):
    total += i

# Python 的内置写法
total = sum(range(1, 101))
```

**两种都要会。** 面试问"求和"时你可以说：
> "Java/C 思路是循环累加；Python 里直接 `sum(range(...))` 就行。
> 不过如果是流式数据或者需要在循环里做别的处理，还是得手写循环。"

这样答，比只写一种显得懂。

### 6. 小细节

- `height = 65.5` —— 数据本身无所谓（自测只查类型），但变量名要配得上含义。
  65.5 米不像身高。**代码是给别人读的**，这是以后写 README、写文档的基本功。
- FizzBuzz 你用 `i % 3 == 0 and i % 5 == 0` 而不是 `i % 15 == 0`。
  **这个没问题**，反而更直白地表表达了意图。

---

---

## Day 1.5 白板自检（2026-09-10，`exercises/whiteboard.py`）

**结论：通过。** 8 分钟独立完成（关掉 AI 补全），逻辑全对。

```python
def FizzBuzz(n):
    result=[]
    for i in range(1,n+1):
        if i%3==0 and i%5==0:
            result.append("FizzBuzz")
        elif i%3==0:
            result.append("Fizz")
        elif i%5==0:
            result.append("Buzz")
        else :
            result.append(i)
    return result
```

### 👍 做得好的地方

用了 `n=16` 和 `n=26` 两个输入 —— **正好跨过 15 和 25 两个边界**。
这说明你有"要测边界"的意识，而没有只测一个 15 就收工。
面试里这是明显的加分项。

### ✏️ 3 处小问题

```python
def FizzBuzz(n):     # 函数名应该小写：def fizzbuzz(n)
                     # PEP 8：函数/变量用 snake_case，类才用 CamelCase
        else :       # 冒号前不要空格：else:
    result=[]        # 运算符两边加空格：result = []
```

> `FizzBuzz` 这个命名不算错（能跑），但第 4 周以后你的代码要给面试官看，
> 命名规范是最容易拿的"印象分"，也最容易丢。

### 🔍 你提到的"列表初始化生疏" —— 这是要补的

见下方「Python 列表初始化速查」，已经给你整理好了。

---

## 需要专门改掉的习惯（跟踪表）

| 习惯 | 出现次数 | 状态 |
|---|---|---|
| 运算符两侧无空格（`result=[]`、`17//5`、`i%3==0`） | 多次 | **待改（已出现 2 天，要盯）** |
| 分号结尾 | 1 | 观察中 |
| 残留 `# TODO` / 死代码 | 1 | 待改 |
| 函数名用了大写驼峰（`FizzBuzz`） | 1 | 观察中 |
| `else :` 冒号前多空格 | 1 | 观察中 |
| 自测过了就以为完全达标 | 1（但很重要） | 已讲解 |

---

## 📌 Python 列表初始化速查（Day 1.5 的缺口）

你说"数组的初始化有点生疏"，这里一次给全。**编程里 90% 的"我好像会用"都栽在初始化上。**

```python
# 1. 空列表（最常用）
result = []
result = list()          # 等价，但没人在意这种写法

# 2. 固定长度、全部填同一个值
zeros = [0] * 5          # -> [0, 0, 0, 0, 0]
nones = [None] * 3       # -> [None, None, None]

# 3. 从 range 生成
nums = list(range(5))    # -> [0, 1, 2, 3, 4]
nums = list(range(1, 6)) # -> [1, 2, 3, 4, 5]

# 4. 列表推导式（Python 的招牌，一定要会）
squares = [x * x for x in range(5)]        # -> [0, 1, 4, 9, 16]
evens = [x for x in range(10) if x % 2 == 0]   # -> [0, 2, 4, 6, 8]

# 5. 二维列表
grid = [[0] * 3 for _ in range(2)]   # -> [[0,0,0], [0,0,0]]  ✅ 正确
```

### ⚠️ 一个经典陷阱（面试常问）

```python
a = [[0] * 3] * 2      # ❌ 看起来一样，其实是 2 个引用指向同一个列表
a[0][0] = 9
print(a)               # -> [[9, 0, 0], [9, 0, 0]]   ← 两个子列表都变了！

b = [[0] * 3 for _ in range(2)]   # ✅ 这样才对
b[0][0] = 9
print(b)               # -> [[9, 0, 0], [0, 0, 0]]
```

**为什么**：`[x] * 2` 复制的是"引用"，不是"内容"。
第一次改 `a[0][0]`，`a[1]` 也跟着变，因为它俩是同一个列表。

面试官问"Python 的浅拷贝和深拷贝"时，答这个例子满分。

---

## 进度总览

| 日期 | 任务 | 结果 |
|---|---|---|
| 09-10 Day 1 | 环境 + 基础语法 + Git | ✅ 6/6，用时约 1 小时 |
| 09-10 Day 1.5 | 白板自检 FizzBuzz（关 AI） | ✅ 8 分钟，逻辑全对 |
| 09-10 Day 2 | dict / JSON / 异常 / 类 | ⏳ 进行中 |

