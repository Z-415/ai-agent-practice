# 问题笔记

> **用途**：练习时卡住的、想不通的、报错搞不明白的 —— 让 AI 助手直接追加到这里。
> **位置**：`code/python-practice/问题笔记.md`
> **纪律**：这里**只记"我不知道的"**，不记代码。代码在 `exercises/dayN.py`。

---

## 🤖 给 AI 助手的指令（把你的 AI 助手指向这一段）

> 请你把下面这条记录**追加到本文档「## 📋 问题清单」的最后一行**（不要覆盖已有内容）。

**格式模板（严格照这个写）：**

```
- **[MM-DD] · DayN · 分类** — 我的问题（用提问者的原话）
  - 答：答案要点，**不超过 3 行**，要具体，不要泛泛而谈
  - 坑：这里最容易错的一步是什么（可选，一句话）
  - 复述：
  - 复查：MM-DD（= 今天 +3 天）
```

**四条硬规则：**

1. **问题用原话**，不要改写成"标准表述"。将来重读时要能认出自己当时在想什么。
2. **答案不超过 3 行。** 超过说明这条该拆成两条，或者你直接讲太长。
3. **「复述」一栏留空**，不要替提问者填。那是他自己要写的 —— 这是判断他真懂没懂的**唯一依据**。
4. **不许写「详见官方文档」** 之类的空话。要么给能懂的解释，要么明确写"这条没讲清"。

> ⚠️ 如果提问者问的是**力扣题解法**，请在「坑」那一栏写明是哪一步的转换没想到
> （比如"没想到用哈希表存 target-num"），不要只贴最终代码。

---

## 分类

| 分类 | 什么时候用 |
|---|---|
| **语法** | 记不清怎么写、为什么这么写（如 `[-kv[1], kv[0]]` 的排序键） |
| **数据结构** | `dict` / `list` / `set` 该选哪个、有什么坑 |
| **报错** | 报错看不懂、环境问题（如 GBK 乱码） |
| **概念** | `self`、`@property`、异常传播这类"原理" |
| **工程** | 文件 / JSON / 异常处理 / venv / Git |
| **力扣** | 算法题：思路没想到、边界漏了 |

---

## 📋 问题清单

> 新的追加到**最下面**。最新的在最后，方便往下翻。

- **[09-10] · Day1 · 报错** — PowerShell 里跑 Python，中文输出变成 `�����Բ�` 怎么办？
  - 答：你的控制台代码页是 936（GBK），Python 默认按系统编码输出。设 `PYTHONUTF8=1` 让 Python 永远用 UTF-8 读写，问题消失。
  - 坑：`chcp 65001` 只对当前窗口生效；要永久解决得设环境变量然后再重开终端。
  - 复述：
  - 复查：09-13

- **[09-10] · Day2 · 概念** — `Conversation` 类里的 `self` 到底是什么？
  - 答：`self` 就是 C 里那个指向自己的指针。`conv.add_user("hi")` 等价于 `add_user(&conv, "hi")` —— Python 把 `&conv` 藏起来自动传，改名叫 `self`。
  - 坑：方法定义时 `self` 必须写在第一个参数位置，但**调用时不用传**，这是最容易忘的一点。
  - 复述：
  - 复查：09-13

- **[09-10] · Day2 · 语法** — `sorted(counts.items(), key=lambda kv: (-kv[1], kv[0]))` 里那个负号为什么能让次数从大到小？
  - 答：`key` 函数是给每个元素算一个"排序分数"，`sorted` 永远按分数**从小到大**排。给分数加负号，原来最大值的分数就变成最小，于是排到了最前面。
  - 坑：元组比较是**从左往右逐个比** —— 先比 `-次数`，相等时才比 `单词`。这就是"次数降序 + 同次数按字母序"能一次写完的原因。
  - 复述：
  - 复查：09-13

- **[09-10] · Day2 · 工程** — `json.dump` / `json.dumps` / `json.load` / `json.loads` 四个到底怎么分？
  - 答：**带 `s` 的是 String**。`dumps` = 对象→字符串，`dump` = 对象→文件；`loads` = 字符串→对象，`load` = 文件→对象。
  - 坑：写中文不加 `ensure_ascii=False`，文件里会变成 `\u5f20\u4e09`（能读回来，但人看不懂）。
  - 复述：
  - 复查：09-13

---

## 🗄️ 已归档（已掌握）

> 当「复述」写出来了、而且 3 天后不看答案能讲清楚 → 把条目剪到这里，并写上日期。

（还没有）

---

## 📊 统计

| 项 | 数 |
|---|---|
| 总条数 | 4 |
| 待复查 | 4 |
| 已归档 | 0 |
| 最常出问题的分类 | 工程 / 语法 / 概念（各 1） |

---

## 💡 怎么用才有效（三条）

1. **每次卡住 → 立刻记，不要攒。** 攒到周末就忘了当时卡在哪，记下来的是"我以为我卡在哪"。
2. **「复述」必须自己写。** 空着的那一栏才是这个文件的全部价值。
   > 让 AI 写答案，你写复述 —— 这就是「AI 当解释器，不当替身」的分工。
3. **复查日只用问自己一句**：不看下面那三行，我能不能讲出来？
   - 能 → 剪到「已归档」
   - 不能 → 把「复查」日期往后推 3 天，**并在「坑」那一栏补一句你这次卡在哪**

> 🔗 相关：`AI使用规则.md`（提问模板：①我在做什么 ②卡在哪 ③我的猜测 ④请指出我错在哪）
> 用那个模板提问，AI 才能写出对你**有用**的答案，而不是一段通用解释。
