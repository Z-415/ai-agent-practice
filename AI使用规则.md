# AI 使用规则（整个 60 天都适用）

> 记录时间：2026-09-10，第 1 周。
> 起因：Day 1 练习 6（FizzBuzz）借助了 VS Code 的 AI 补全才写出来。

---

## 为什么要专门立这个规矩

你在学的东西叫 **AI Agent**。它是一个"帮你写代码的工具"。
而你现在最大的风险是：**这个工具会让你误以为自己学会了。**

机制很简单：

```
你自己想 → 卡住 → 自己再想 → 想通 → 写出来     ← 这个循环产生"能力"
你自己想 → 卡住 → AI 补全 → 通过自测           ← 这个循环只产生"进度"
```

自测都是绿的，**但两个循环的结果完全不一样**。第二种情况下，你拿到的是"代码跑通了"，不是"我会了"。

而且这个坑有延迟性：**它现在不疼，第 11 周手写 ReAct Agent 的时候才疼**——
那时候没人给你补全，你得从零想清楚"循环怎么退出""工具调用解析失败怎么办"。

---

## 两条轨道，严格分开

### 🎓 学习轨道（写练习、写项目的核心逻辑）

**关掉 AI 补全。** AI 只做两件事：

1. **当解释器**：问"`@property` 是什么，举个例子"
2. **当审查者**：自己写完 → 贴给 AI（或我）→ 让它指出问题

> 本仓库已配置好：`.vscode/settings.json` 会在 `code\python-practice` 里自动关掉 AI 内联补全。
> 你其他文件夹不受影响。

### 🏭 生产轨道（写脚手架、配置、重复劳动）

**随便用 AI。** 没人要求你手写 Dockerfile、nginx 配置、CI 脚本。
第 4 周以后的项目里，这类代码尽管让 AI 生成。

判断标准：**这段代码是在"训练你"，还是在"服务项目"？**

---

## 允许 / 不允许（仅限学习轨道）

| ✅ 可以做 | ❌ 不可以做 |
|---|---|
| 问"这个语法是什么，举个例子" | 让 AI 直接写出答案 |
| 自己先写完整版，再让 AI 审查 | 卡住 5 分钟就让 AI 写整个函数 |
| 把报错原样贴给 AI，让它解释为什么 | 接受 Tab 补全吐出来的逻辑 |
| 让 AI 帮你写注释、README、文档 | 让 AI 写算法主体 |
| 让 AI 出题考你 | 让 AI 替你"想" |

**唯一例外**：真的卡超过 40 分钟了（规则 4），可以要答案 —— 但**要完之后必须合上文件，自己从空白重写一遍**。

---

## 唯一的硬标准

> ### 合上电脑，拿张纸，能把刚才那段代码默写出来吗？

写不出来 = 那段逻辑还不是你的。

这不是打击你，是**给你一把随时能用的尺子**。第 5 周以后你每天都在跟 AI 协作，
这把尺子是你唯一能防止"12 周后简历写不出真东西"的东西。

---

## 面试为什么认这个

面试 = 白板 / 共享文档 / 屏幕共享。**没有补全。**

面试官会说：
- "你写一下这个函数的实现"（看你手会不会动）
- "这里为什么用 `dict` 不用 `list`？"（看你脑子有没有数）
- "如果输入是空的会怎样？"（看你有没有边界意识）

这些能力全部来自"自己想过一遍"。补全通过的代码，你答不上第二句。

---

# 🔍 查文档 vs 问 AI 的顺序（2026-09-10 补充）

**可以问 AI。但顺序不能反。**

## 四步阶梯（从上往下走，不要跳）

| 顺序 | 做什么 | 时间 |
|---|---|---|
| 1 | **先读你刚才的报错** | 30 秒 |
| 2 | **查官方文档** | 10 分钟 |
| 3 | **问 AI 当"解释器"** | — |
| 4 | **卡满 40 分钟 → 才允许要答案** | — |

> **第 4 步要完答案，必须合上文件、从空白重写一遍。**
> 看了答案再抄一遍不算。

## 为什么官方文档排在第 2 位（比 AI 靠前）

1. **文档最快最准。** "dict 有哪些方法"，文档一页讲完；问 AI 你会拿到一段话，还可能夹着错的。
2. **AI 会自信地编。** 证据就在眼前：我昨天写了"LC1 暴力解会超时"，
   实测 n=10⁴ 只要 1.54 秒（力扣上限就是 10⁴），**根本不会超时**。我自己就编了。
3. **文档不会随模型版本变。** 今天问出来的答案，明年可能就不对了。
4. **面试官会问"你平时怎么解决问题"。** "我会读官方文档"是及格答案；"我问 AI"是扣分答案。

## 提问模板（这其实是第 3-4 周"提示词工程"的预告）

### ❌ 坏问题

- "类怎么写？"  ← 太宽，AI 只能给你一段通用解释，跟你的实际困难无关
- "这个报错怎么办？"  ← 没贴报错，AI 只能猜
- "帮我把这个函数写出来"  ← 这是要答案，不是学习

### ✅ 好问题（四要素）

> **① 我在做什么　② 卡在哪　③ 我的猜测　④ 请指出我错在哪**

**示例：**

> 我在写一个 `Conversation` 类，`__init__` 和 `add_user` 已经写好了。
> 我发现 `self._messages` 能跑通，但把下划线去掉写成 `self.messages` 就出问题。
> 我猜下划线是"私有"的意思，但 Python 里好像没有真正的私有？
> 请指出我的理解哪里不对，每种情况各举一个例子。

**为什么这个模板管用：**

- 它**逼你先自己想**。哪怕想错了，AI 也是在纠正你的**思路**，而不是替你思考。
- AI 会针对你的**实际误解**回答，而不是给你一段无关的通用教程。

## 最后一条：AI 的答案必须"可验证"

| 验证手段 | 适用 |
|---|---|
| **跑一遍** | 代码类 —— 最快，立刻出结果 |
| **查文档对一遍** | 概念 / API 类 |
| **贴给我** | 拿不准的 —— 但我也会错（见上面第 2 条） |

> **不能用"AI 说它对"当验证。** 包括我说的。

---

## 进度记录

| 日期 | 用了 AI 补全吗 | 备注 |
|---|---|---|
| 2026-09-10（Day 1） | ⚠️ 练习 6 用了 | 本人主动说明，已计入校准 |
| 2026-09-10（Day 2 起） | 已通过 `.vscode/settings.json` 关闭 | 待观察 |
